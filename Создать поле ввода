&НаСервере
Процедура СоздатьПолеВвода(ЗаголовокПоля, НазваниеЭлемента, НазваниеРеквизита, НазваниеОбработчика)
	
	// Проверка на попытку создания эллемента с одним и тем же именем
	Если Элементы.Найти(НазваниеЭлемента) = Неопределено Тогда
		ТипыРеквизита = Новый Массив;
		ТипыРеквизита.Добавить(Тип("Строка"));
		ОписаниеТиповДляРеквизита = Новый ОписаниеТипов(ТипыРеквизита);	
		НовыйРеквизит = Новый РеквизитФормы(НазваниеРеквизита, ОписаниеТиповДляРеквизита, ,ЗаголовокПоля, Истина);                                                   	
		ДобавляемыеРеквизиты = Новый Массив;                                                  
		ДобавляемыеРеквизиты.Добавить(НовыйРеквизит); 
		ИзменитьРеквизиты(ДобавляемыеРеквизиты);
		
		// Добавить элемент формы и связать его с реквизитом.
		НовыйЭлемент = Элементы.Добавить(НазваниеЭлемента, Тип("ПолеФормы"));
		НовыйЭлемент.ПутьКДанным = НазваниеРеквизита; 
		НовыйЭлемент.Вид = ВидПоляФормы.ПолеВвода;
		НовыйЭлемент.КнопкаОчистки = Истина;  
		
		// Установить обработчик события.
		НовыйЭлемент.УстановитьДействие("ПриИзменении", НазваниеОбработчика);
	КонецЕсли;
	
КонецПроцедуры 


&НаКлиенте
Процедура ОбработчикПоляГода(Элемент)
		
	Год = Число(ЭтотОбъект["РеквизитГод"]);
	
	Если (Год%4 = 0 И Не Год % 100 = 0) ИЛИ (Год%100 = 0 И Год%400 = 0) Тогда
		Сообщение("Этот год високосный");
	Иначе Сообщение("Этот год не високосный");
	КонецЕсли;
	
КонецПроцедуры    
